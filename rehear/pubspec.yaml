name: rehear
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1 # Keeping the original SDK constraint

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  record: ^6.0.0
  just_audio: ^0.10.4
  file_picker: ^10.2.0
  flutter_riverpod: ^2.6.1
  audio_waveforms: ^1.3.0
  permission_handler: ^12.0.1
  uuid: ^4.5.1
  ffmpeg_kit_flutter: ^5.1.0
  path_provider: ^2.1.1
  path: ^1.8.3
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Updated

flutter:
  uses-material-design: true